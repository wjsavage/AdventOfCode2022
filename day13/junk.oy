    if isinstance(first, list) and isinstance(second, list):
        return list(filter(lambda x: x is not None, [is_less_than(a, b) for a, b in zip(first, second)]))[0]

    if not first:
        return True
    if first and not second:
        return False

    if isinstance(first, int) and isinstance(second, int):
        if first == second:
            return None
        return first < second